name: Build Linux/Windows release

on:
  push:
    branches:
      - main  # Change this to your main branch name

permissions:
  contents: write
  
jobs:
  build-linux-windows-release:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Generate Windows Binary
      uses: JackMcKew/pyinstaller-action-windows@python3-10-pyinstaller-5-3
      with:
        path: src
        spec: app.spec

    - name: Generate Linux Binary
      uses: JackMcKew/pyinstaller-action-linux@python3.10
      with:
        path: src
        spec: app.spec

    - name: Upload artifacts
      uses: actions/upload-artifact@v2
      with:
        name: dist
        path: |
          src/dist/windows/*
          src/dist/linux/*
          
  build-macos-release:
    runs-on: macos-13
    env:
      working-directory: src
    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Install Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.10'

    - name: Install requirements
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Generate MacOS Binary
      run: |
        pyinstaller app.spec

    - name: Upload artifacts
      uses: actions/upload-artifact@v2
      with:
        name: dist
        path: dist/*

  build-all-release:
    needs: [build-linux-windows-release, build-macos-release]
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Download artifacts
      uses: actions/download-artifact@v2
      with:
        name: dist

    - name: Create Release
      uses: softprops/action-gh-release@v1
      with:
        files: |
          dist/*
        tag_name: ${{ github.sha }}
        body: |
          Release ${{ github.sha }}
        draft: false
        prerelease: false
        token: ${{ secrets.GITHUB_TOKEN }}